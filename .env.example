# Copy this file to `.env` and fill in the values.
# These are public (the anon key is safe to ship — RLS policies enforce security).
# `.env` is gitignored; `.env.example` is committed.

VITE_SUPABASE_URL=https://jmikulhgpgfaarqzwrgl.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Ysk7PrEmW07t-U_h-aUCKA_2mHttSKY

# GoatCounter site code for privacy-respecting analytics.
# Leave blank for local development; set in production.
VITE_GOATCOUNTER_CODE=
